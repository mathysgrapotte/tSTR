process {
    withName: "FIND_SEQUENCE_IN_BED" {
    publishDir=[
        path: { "${params.outdir}"},
        overwrite: true
    ]
    }
}

process {
    withName: "FIND_SEQUENCE_IN_FASTA" {
    publishDir=[
        path: { "${params.outdir}"},
        overwrite: true
    ]
    }
}

process {
    withName: "FILTER_VCF_SEQUENCE_IN_BED" {
    publishDir=[
        path: { "${params.outdir}"},
        overwrite: true
    ]
    }
}

process {
    withName: "BUILD_INDIVIDUAL_FASTA" {
    publishDir=[
        path: { "${params.outdir}"},
        overwrite: true
    ]
    }
}

process {
    withName: "MAKE_PREDICTIONS" {
    publishDir=[
        path: { "${params.outdir}"},
        overwrite: true
    ]
    }
}

process {
    withName: "FIND_GENE_IN_GCT_GTEX" {
    publishDir=[
        path: { "${params.outdir}"},
        overwrite: true
    ]
    }
}