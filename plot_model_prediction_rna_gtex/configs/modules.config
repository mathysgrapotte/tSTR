

process {
    withName: "MERGE_DATASET" {
    publishDir=[
        path: { "${params.outdir}"},
        overwrite: true
    ]
    }
}

process {
    withName: "MAKE_SCATTER" {
    publishDir=[
        path: { "${params.outdir}/plots/"},
        overwrite: true
    ]
    }
}